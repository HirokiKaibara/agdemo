# 提案用入力メモ

更新日: 2026-04-30  
用途:  本機能のデモ紹介の為の資料
前提: `README` と現行ソースコードで確認できる内容のみ記載

## 1. 先方要望

- Excel VBA / Access VBA の現状を短時間で把握したい
- 段階的なリファクタリング方針を見たい

## 2. 結論

- 現実的な方式は `VBA読込 -> 手続き要約 -> ファイル要約 -> プロジェクト統合解析 -> 4成果物確認` の半自動方式
- 現在のデモでは、複数 VBA ファイルを対象に、現状仕様書 / 問題点分析 / 詳細リファクタリング案 / リファクタリング設計書を生成し、Word / PDF 出力まで流せる

## 3. 実現方法

### 推奨方式

- 社内向け Web アプリ
- 流れ:
  1. VBA ファイルを読み込む、またはコードを貼り付ける
  2. 案件名 / システム名を入力する
  3. Gemini API で手続き単位、ファイル単位、プロジェクト単位の順に解析する
  4. 現状仕様書で、現行フロー、機能、テーブル / カラム、依存関係を確認する
  5. 問題点分析で、重大度と優先度付きの論点を確認する
  6. 詳細リファクタリング案で、代替案と段階的ロードマップを確認する
  7. リファクタリング設計書で、目標構成と移行方針を確認する
  8. 必要に応じて Word / PDF を出力する

### 現デモで確認できていること

- `.bas` `.cls` `.frm` `.txt` の読込
- 複数ファイルアップロード
- 選択中ファイルのコード表示切り替え
- Gemini API による解析
- 手続き単位要約
- ファイル単位要約
- プロジェクト単位統合解析
- 現状仕様書の表示
- 問題点分析の表示
- 詳細リファクタリング案の表示
- リファクタリング設計書の表示
- Mermaid 図の表示
- テーブル / カラムの一覧表示
- 段階的ロードマップの表示
- Word 出力
- PDF 出力
- キャッシュ再利用

## 4. 実現可能性の判断

| 観点 | 判断 | 補足 |
|---|---|---|
| VBA読取 | 可能 | `.bas` `.cls` `.frm` `.txt` の読込とコード貼り付けに対応 |
| 複数ファイル解析 | 可能 | 複数ファイルをまとめて 1 回の解析対象にできる |
| 現状把握の文書化 | 可能 | 現状仕様書として画面表示と Word / PDF 出力ができる |
| 問題点整理 | 可能 | 重大度、優先度、影響、推奨アクションを整理できる |
| 改修計画のたたき台化 | 可能 | 段階的ロードマップ付きで提示できる |
| 設計書たたき台化 | 可能 | 目標アーキテクチャ、モジュール責務、移行フローを出せる |
| 完全自動リファクタリング | 現時点では難しい | 本デモは改修のたたき台生成が目的で、自動修正完了までは対象外 |

## 5. 現在の懸念・問題

| 問題 | 内容 |
|---|---|
| 実行時依存の断定不可 | コードだけでは運用ルール、実行時設定、周辺システム依存を完全断定できない |
| 業務仕様の断定不可 | 生成結果はレビュー前提であり、そのまま確定仕様にはならない |
| 外部連携の検出限界 | HTTP/HTTPS 呼び出しがコードに現れない場合、エンドポイントは検出できない |
| テーブル抽出の前提 | テーブル / カラムは主に SQL 文字列から抽出するため、SQL がコードに無いと拾えない |
| Gemini API 依存 | API 制限や一時混雑時には再試行やフォールバックが必要になる |

## 6. クライアント側で改善すると効果が大きいこと

| 項目 | 推奨内容 | 期待効果 |
|---|---|---|
| 対象範囲 | 関連する VBA ファイル一式をまとめて提供する | 依存関係や役割分担を整理しやすくなる |
| ファイル整理 | 業務単位、サブシステム単位でファイル群を分ける | 解析結果が会議で読みやすくなる |
| 案件名入力 | 案件名 / システム名を明示する | 出力資料のタイトルや説明が分かりやすくなる |
| 補足資料 | 運用ルールや実行時依存を別紙で補足する | 要確認事項を減らし、レビューしやすくなる |
| SQL記述 | SQL 文字列や依存先が分かるコードを含める | テーブル / カラム抽出の精度が上がりやすい |

## 7. こちら側で対応できること

| 項目 | 対応内容 |
|---|---|
| 手続き要約 | `Sub` / `Function` 単位で処理概要を整理する |
| ファイル整理 | ファイルごとの役割、依存関係、主要手続きを整理する |
| 現状把握 | 現状仕様書としてフロー、機能、依存関係をまとめる |
| 問題点整理 | 重大度、優先度、影響、推奨アクションを整理する |
| 改修計画 | 段階的ロードマップと代替案を提示する |
| 設計たたき台 | 目標アーキテクチャ、モジュール責務、移行フローを提示する |
| テーブル分析 | SQL 文字列からテーブル / カラムを抽出して一覧化する |
| 文書化 | 4 成果物を Word / PDF で出力する |
| 再解析効率化 | キャッシュ再利用で同一入力の再解析を速くする |

## 8. 問題への対応方法と代替案

| 問題 | 対応方法 | 代替案 |
|---|---|---|
| 実行時依存の断定不可 | 要確認前提で出し、レビューで補完する | 補足資料やヒアリング結果を別途整理する |
| 複数ファイルの関係整理 | 複数ファイルをまとめて解析し、役割と依存関係を整理する | 初期は業務単位ごとに分けて投入する |
| 外部連携の検出限界 | コード中の呼び出し痕跡を根拠に表示する | 外部仕様書や設定情報を別途確認する |
| テーブル抽出の限界 | SQL 文字列から抽出した結果を優先表示する | DB 設計書と突合して補完する |
| API 混雑 | 自動リトライとモデルフォールバックで完走率を上げる | 時間をおいて再実行する |
| 完全自動改修の期待 | たたき台生成と確認フロー用途として説明する | 設計・実装は次フェーズで別途進める |

## 9. 営業提案で使いやすい伝え方

### 言ってよいこと

- `複数の VBA ファイルから、現状把握から設計たたき台までの流れはデモとして成立している`
- `4 成果物を順番に確認し、そのまま Word / PDF に落とせる`
- `SQL 文字列からテーブル / カラムを抽出して一覧化できる`
- `段階的ロードマップ付きで改修の進め方を議論しやすい`
- `本番導入は、現状把握と確認フローを早める半自動方式が現実的`

### 言い切らない方がよいこと

- `どんな VBA でも完全に仕様を断定できる`
- `実行時依存や運用ルールまで全自動で再現できる`
- `そのまま安全に自動リファクタリングまで完了できる`
- `すべての外部連携やテーブルが必ず抽出できる`

## 10. 根拠資料

- [README.md](./README.md)
- [営業資料_レガシーコード解析デモ.md](./営業資料_レガシーコード解析デモ.md)
- [public/index.html](./public/index.html)
- [public/app.js](./public/app.js)
- [src/web/server.ts](./src/web/server.ts)
- [src/web/analysisRenderer.ts](./src/web/analysisRenderer.ts)
- [src/web/documentExport.ts](./src/web/documentExport.ts)
- [src/legacy-analyzer/analyzer.ts](./src/legacy-analyzer/analyzer.ts)
- [src/legacy-analyzer/tableReferenceExtractor.ts](./src/legacy-analyzer/tableReferenceExtractor.ts)
- [src/legacy-analyzer/types.ts](./src/legacy-analyzer/types.ts)
